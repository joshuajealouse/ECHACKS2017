package starynight;

public class Calcluations {

	public double ranking (String cityName, String windSpeed, String maxTemp, String minTemp, String descripCloud, String valueCloud, String weatherType, String airPressure)
	{
		double calculation = 0;
		int a, b, c, d, e, f, g, h;
		a = Integer.parseInt(cityName);
		b = Integer.parseInt(windSpeed);
		c = Integer.parseInt(maxTemp);
		d = Integer.parseInt(minTemp);
		e = Integer.parseInt(descripCloud);
		f = Integer.parseInt(valueCloud);
		g = Integer.parseInt(weatherType);
		h = Integer.parseInt(airPressure);
		
		return calculation;
	}
}
